services:
  app:
    build: .
    image: mail-exclude-tool
    container_name: mail-exclude-tool
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
